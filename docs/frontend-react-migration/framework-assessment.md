# Отчёт по фреймворкам и целевому стеку MES

Дата: 2026-07-19
Состояние: фактический код ветки `codex/frontend-react-migration`; Pilot не переключён

| Слой | Что используется сейчас | Что хорошо | Что плохо / риск | На что менять | Текущий вывод |
| --- | --- | --- | --- | --- | --- |
| Основной frontend | Vanilla JavaScript, ES modules, ручной HTML/DOM-рендеринг | Нет зависимости от тяжёлого runtime; существующая бизнес-логика проверена большим набором QA | Состояние, DOM и события связаны вручную; модули визуально расходятся; трудно безопасно переиспользовать компоненты и типизировать контракты | React + TypeScript через вертикальные islands, без переписывания API и бизнес-логики одним релизом | Менять поэтапно. Уже production-интегрированы 20 read-only сценариев за default-off gates; legacy остаётся rollback и владельцем записей |
| Новый frontend | React 19 + React DOM 19 + TypeScript/TSX | Декларативный UI, локальное состояние, типизированные adapters, единый lifecycle и error fallback | Пока это параллельный слой: дублируются legacy и React-представления; команды ещё не имеют полной parity | Оставить React + TypeScript целевым стеком; после Pilot-приёмки переносить команды по одному пользовательскому сценарию | Хороший целевой выбор. Не включать глобально до authenticated Pilot QA и write-parity |
| UI-компоненты | Собственный набор `OperationalPage`, `ModuleHeader`, `Panel`, `MetricGrid`, `StatusToken`, `ActionButton` и CSS-контракты | Сохраняет текущий MES-язык и позволяет унифицировать разнородные модули без внешней дизайн-системы | Набор ещё не покрывает все сложные редакторы, таблицы и Gantt; возможны локальные CSS-исключения | Развивать собственную typed design system поверх React; не менять сейчас на MUI/Ant/Bootstrap | Оставить и расширять. Внешняя библиотека сейчас добавит визуальный разрыв и стоимость миграции |
| Сборка | esbuild | Быстрая, простая, уже собирает независимые lazy islands с hash-версиями | Нет готовой экосистемы маршрутизации/dev-server/HMR уровня метафреймворков | Пока оставить esbuild; оценивать Vite только после сокращения legacy bootstrap | Замена сейчас не нужна: узкое место — архитектура UI, не скорость сборки |
| Сервер приложения | Node.js ES modules, собственный HTTP/runtime слой без Express/Nest | Минимум зависимостей; существующие API и release-процедуры стабильны | Много инфраструктурного кода поддерживается вручную; изменение затронет все домены и эксплуатацию | Не менять в рамках frontend-цели. Отдельно оценивать Fastify/Nest только при доказанной серверной проблеме | Оставить. Переход frontend не требует смены серверного фреймворка |
| Данные | PostgreSQL через пакет `postgres` | Все четыре домена уже работают от PostgreSQL; есть серверные read models, команды и parity-gates | Shared-state/bootstrap ещё существует как совместимый аварийный механизм; frontend должен корректно ждать bounded projections | PostgreSQL оставить authority; постепенно удалять только доказанно неиспользуемые compatibility-пути | Хорошо. Не заменять; завершать потребление серверных projections каждым UI-сценарием |
| Иконки | `lucide-react` плюс собственный registry | Единый React API и контролируемые MES-алиасы | В legacy остаются собственные SVG/HTML-пути; возможны расхождения имён и размеров | Сводить новые экраны к registry и токенам, сохраняя нужные фирменные иконки | Оставить, унифицировать постепенно |
| QA | Node-скрипты, Chromium CDP, контрактные и визуальные проверки | Проверяет реальные DOM-сценарии, fallback, отсутствие записей, bundle и commit telemetry | Много специализированных скриптов; поддержка дороже стандартного unit/e2e-набора | Сохранить текущие acceptance-gates; позднее добавить Vitest/Playwright для новых React-компонентов, не удаляя критические CDP-проверки | Добавлять стандартные тесты постепенно; текущий QA пока является важной страховкой миграции |

## Пользовательский результат

Сам по себе технический переход не должен резко перекрасить продукт. Пользователь
получает более согласованные карточки, таблицы, статусы и действия, сохранение
выбора без полной перерисовки страницы, более предсказуемые ошибки и возможность
быстрее исправлять один общий компонент во всех мигрированных модулях.

На переходном этапе соседние модули могут выглядеть по-разному. Это допустимо,
если каждый новый вертикальный сценарий использует общий UI-контракт, сравнивается
с legacy на одинаковых данных и имеет мгновенный rollback. Сначала унифицируется
компонентный язык, затем — оставшиеся экраны; ожидание полной визуальной
унификации legacy до старта лишь законсервировало бы расхождения.

## Что ещё не завершено

- authenticated Pilot QA и решение о default-on для уже встроенных islands;
- перенос write-команд: создание, редактирование, назначение, факт, публикация;
- крупные самостоятельные поверхности: Gantt, Specifications 2.0 и прототип
  авторизации; Contour Admin уже production-интегрирован default-off;
- удаление legacy возможно только после функциональной parity, Pilot-приёмки и
  проверенного rollback для каждого сценария.
