import { mountReactIsland, type ReactMigrationIslandOptions } from "./island-runtime";
import { ContourAdminScenario, type ContourAdminReactCommand } from "./modules/contour-admin/ContourAdminScenario";
export function mountContourAdminReactIsland(target: HTMLElement, initialPayload: unknown, options: ReactMigrationIslandOptions & { onCommand?(command: ContourAdminReactCommand): Promise<{ ok?: boolean; label?: string; message?: string; code?: number | string; durationMs?: number } | void> } = {}) { const { onCommand, ...runtime } = options; return mountReactIsland(target, (payload) => <ContourAdminScenario payload={payload} onCommand={onCommand} />, initialPayload, runtime); }
