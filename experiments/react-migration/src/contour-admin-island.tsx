import { mountReactIsland, type ReactMigrationIslandOptions } from "./island-runtime";
import { ContourAdminScenario, type ContourAdminReactCommand } from "./modules/contour-admin/ContourAdminScenario";
export function mountContourAdminReactIsland(target: HTMLElement, initialPayload: unknown, options: ReactMigrationIslandOptions & { onRequestLegacy?(scope?: string): void; onCommand?(command: ContourAdminReactCommand): Promise<{ ok?: boolean; label?: string; message?: string; code?: number | string; durationMs?: number } | void> } = {}) { const { onCommand, onRequestLegacy, ...runtime } = options; return mountReactIsland(target, (payload) => <ContourAdminScenario payload={payload} onCommand={onCommand} onRequestLegacy={onRequestLegacy} />, initialPayload, runtime); }
