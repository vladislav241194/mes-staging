import { mountReactIsland, type ReactMigrationIslandOptions } from "./island-runtime";
import { PlanningWorkbenchScenario, type PlanningWorkbenchReactCommand, type PlanningWorkbenchReactNavigation } from "./modules/planning-workbench/PlanningWorkbenchScenario";
export interface PlanningWorkbenchIslandOptions extends ReactMigrationIslandOptions { onCommand?(command: PlanningWorkbenchReactCommand): Promise<{ ok?: boolean; message?: string } | void>; onNavigate?(navigation: PlanningWorkbenchReactNavigation): Promise<{ ok?: boolean; message?: string } | void> }
export function mountPlanningWorkbenchReactIsland(target: HTMLElement, initialPayload: unknown, options: PlanningWorkbenchIslandOptions = {}) { const { onCommand, onNavigate, ...runtimeOptions } = options; return mountReactIsland(target, (payload) => <PlanningWorkbenchScenario payload={payload} onCommand={onCommand} onNavigate={onNavigate} />, initialPayload, runtimeOptions); }
