import { mountReactIsland, type ReactMigrationIslandOptions } from "./island-runtime";
import { StructureEquipmentScenario, type StructureEquipmentReactCommand } from "./modules/structure-equipment/StructureEquipmentScenario";
export interface StructureEquipmentIslandOptions extends ReactMigrationIslandOptions { onCommand?(command: StructureEquipmentReactCommand): Promise<{ ok?: boolean; id?: string; message?: string } | void>; onRequestLegacy?(scope?: string): void }
export function mountStructureEquipmentReactIsland(target: HTMLElement, initialPayload: unknown, options: StructureEquipmentIslandOptions = {}) { const { onCommand, onRequestLegacy, ...runtimeOptions } = options; return mountReactIsland(target, (payload) => <StructureEquipmentScenario payload={payload} onCommand={onCommand} onRequestLegacy={onRequestLegacy} />, initialPayload, runtimeOptions); }
