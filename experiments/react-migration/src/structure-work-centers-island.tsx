import { mountReactIsland, type ReactMigrationIslandOptions } from "./island-runtime";
import { StructureWorkCentersScenario, type StructureWorkCentersReactCommand } from "./modules/structure-work-centers/StructureWorkCentersScenario";
import type { StructureRegistryId } from "./modules/structure-employees/adapter";
export interface StructureWorkCentersIslandOptions extends ReactMigrationIslandOptions { onCommand?(command: StructureWorkCentersReactCommand): Promise<{ ok?: boolean; id?: string; message?: string } | void>; onNavigateRegistry?(registryId: StructureRegistryId): void }
export function mountStructureWorkCentersReactIsland(target: HTMLElement, initialPayload: unknown, options: StructureWorkCentersIslandOptions = {}) { const { onCommand, onNavigateRegistry, ...runtimeOptions } = options; return mountReactIsland(target, (payload) => <StructureWorkCentersScenario payload={payload} onCommand={onCommand} onNavigateRegistry={onNavigateRegistry} />, initialPayload, runtimeOptions); }
