import { mountReactIsland, type ReactMigrationIslandOptions } from "./island-runtime";
import { StructureOrgUnitsScenario, type StructureOrgUnitsReactCommand } from "./modules/structure-org-units/StructureOrgUnitsScenario";
export interface StructureOrgUnitsIslandOptions extends ReactMigrationIslandOptions { onCommand?(command: StructureOrgUnitsReactCommand): Promise<{ ok?: boolean; id?: string; message?: string } | void>; onRequestLegacy?(scope?: string): void }
export function mountStructureOrgUnitsReactIsland(target: HTMLElement, initialPayload: unknown, options: StructureOrgUnitsIslandOptions = {}) { const { onCommand, onRequestLegacy, ...runtimeOptions } = options; return mountReactIsland(target, (payload) => <StructureOrgUnitsScenario payload={payload} onCommand={onCommand} onRequestLegacy={onRequestLegacy} />, initialPayload, runtimeOptions); }
