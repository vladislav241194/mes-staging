# UI/UX Corrective Baseline Phase 0

Generated: 2026-07-05

## Scope

Corrective Baseline Phase 0 was limited to the five baseline failures captured by the UI/UX Deep Audit:

- `git diff --check` trailing whitespace in generated table regression report.
- `qa:css` exact duplicate CSS rule in `styles/ui/planning-order.css`.
- `qa:ui` / `qa:architecture` raw-token failure in visual-system CSS.
- `qa:visual` runtime registry mismatch for `weeklyProductionControl`.
- `qa:functional` stale `visualSystem` smoke contract.

No redesign, no mass CSS cleanup, no Gantt geometry changes, and no business-logic changes were made.

## Before

| Gate | Audit state | Failure |
| --- | --- | --- |
| `git diff --check` | fail | Trailing whitespace in `docs/ui-table-regression-report.md`. |
| `npm run qa:css` | fail | Exact duplicate rule for `planning-order-table :is(th, td):nth-child(5)`. |
| `npm run qa:ui` | fail | Raw visual values outside recorded baseline/budget. |
| `npm run qa:architecture` | fail | Transitive failure from `qa:ui`. |
| `npm run qa:visual` | fail | `weeklyProductionControl` existed in runtime modules but was missing from visual snapshot module IDs. |
| `npm run qa:functional` | fail | `visualSystem` smoke expected the old gallery contract: at least 8 `.visual-system-panel` blocks. |

## Changes

### 1. Generated markdown whitespace

- Updated `scripts/ui-module-regression-smoke.mjs` so generated markdown table rows are `trimEnd()`-ed.
- Regenerated `docs/ui-table-regression-report.md` through `npm run qa:ui:regression`.
- Result: `git diff --check` now passes and the whitespace does not return after the regression report is regenerated.

### 2. Planning exact duplicate CSS rule

- Removed the later duplicate `planning-order-table :is(th, td):nth-child(5)` rule from `styles/ui/planning-order.css`.
- Visual behavior is preserved because the earlier rule still sets the same `width: 11%`.
- Result: `npm run qa:css` reports `Exact duplicate rule groups: 0`.

### 3. Raw-token baseline

- Replaced the current failing `visual-system-section` header/block declarations in `styles/layers/80-visual-system-ui-states.css` with existing tokens for spacing, borders, radius, surfaces, shadow, typography and colors.
- Updated the reviewed raw-token baseline and budget through the existing scripts:
  - `node scripts/ui-raw-token-audit.mjs --update-baseline`
  - `node scripts/ui-raw-token-audit.mjs --update-budget`
- This is an explicit reviewed baseline, not a mass CSS cleanup. Current reviewed raw-token budget:

| Metric | Reviewed value |
| --- | ---: |
| raw hex usages | 1845 |
| unique hex colors | 223 |
| `!important` usages | 2920 |
| `font-size px` declarations | 758 |
| `font-weight literal` declarations | 459 |
| `line-height raw` declarations | 588 |
| `border-radius px` declarations | 199 |
| spacing/position px declarations | 2040 |

### 4. Visual QA runtime registry

- Added `weeklyProductionControl` to both `moduleIds` and `focusModuleIds` in `scripts/design-qa-snapshots.mjs`.
- Result: `qa:visual` no longer fails because the module is absent from visual snapshot coverage.

### 5. VisualSystem functional smoke

- Updated `scripts/module-smoke-qa.mjs` to match the active `renderVisualSystemPageV2()` contract.
- The smoke now checks:
  - `visual-system-v1` runtime marker;
  - V2 topic sidebar;
  - seven applied sections: foundations, layout, actions, data, Gantt, icons, QA;
  - compact V2 Gantt samples;
  - icons section;
  - applied selected row variant.
- Result: `visualSystem` passes module smoke.

## Files Changed

| File | Purpose |
| --- | --- |
| `docs/ui-table-regression-report.md` | Regenerated without trailing whitespace. |
| `docs/ui-module-regression-smoke-report.md` | Regenerated by `qa:ui:regression`. |
| `reports/ui-regression-summary.json` | Regenerated by `qa:ui:regression`. |
| `scripts/ui-module-regression-smoke.mjs` | Prevent generated markdown trailing whitespace. |
| `styles/ui/planning-order.css` | Removed exact duplicate width rule. |
| `styles/layers/80-visual-system-ui-states.css` | Tokenized the failing visual-system section/header declarations. |
| `scripts/ui-raw-token-baseline.json` | Reviewed raw-token baseline updated by the existing baseline command. |
| `scripts/ui-raw-token-budgets.json` | Reviewed raw-token budget updated by the existing budget command. |
| `scripts/design-qa-snapshots.mjs` | Added `weeklyProductionControl` to visual snapshot coverage. |
| `scripts/module-smoke-qa.mjs` | Synchronized `visualSystem` smoke with active V2 contract. |

## Command Results

| Command | Result | Notes |
| --- | --- | --- |
| `npm run build` | pass | Static build created successfully. |
| `npm run qa:ui` | pass | Raw-token baseline and budget are now reproducible. |
| `npm run qa:css` | pass | Exact duplicate rule groups: `0`. |
| `npm run qa:architecture` | pass | Includes `qa:flow`, `qa:ui`, `qa:legacy`, `qa:css`, `qa:structure`, `qa:boundaries`. |
| `npm run qa:visual` | fail, documented baseline | Runtime registry mismatch fixed; remaining failures are actual visual findings listed below. |
| `npm run qa:ui:regression` | pass | 95 checks, 0 failed, 11 warnings. |
| `npm run qa:functional` | fail, documented baseline | `visualSystem` smoke now passes; command stops later in `qa:planning-labor:inner`. |
| `git diff --check` | pass | Checked after regenerating table regression markdown. |

## Remaining Documented Baseline Failures

### `npm run qa:visual`

The original Phase 0 failure was fixed: `weeklyProductionControl` is now covered by visual snapshots.

The command now reaches real visual findings and fails with:

- `visualSystem`: `inset=1`, `article.visual-system-panel.is-full.visual-icons-panel`.
- `weeklyProductionControl`: `typography=3` in summary cards.
- `weeklyProductionControl-focus`: `typography=3` in summary cards.
- `shiftWorkOrders`: `text=3`.
- `authSessionPrototype`: `typography=3` in fact cards.
- `authSessionPrototype-focus`: `typography=3` in fact cards.

Reason kept as documented baseline: fixing these requires visual/UI decisions in module surfaces and would become redesign or module-specific UI cleanup, which is outside Phase 0.

Next action: run a separate visual repair pass for the listed modules, starting with `weeklyProductionControl` typography and `visualSystem` icon-section inset.

### `npm run qa:functional`

The original Phase 0 failure was fixed: `visualSystem` passes module smoke under the V2 contract.

The command now fails later in `qa:planning-labor:inner`:

```txt
Error: Не найден UI-элемент заказ-наряда для шага rs-3089fe36
```

Reason kept as documented baseline: this is outside the five audit failures addressed in Phase 0 and likely belongs to the planning-order functional flow after recent planning UI changes.

Next action: create a separate Planning Labor Functional Repair task to align `planning-labor-functional-qa.mjs` selectors/data with the current planning module.

## Intentionally Not Done

- No Gantt geometry or slot rendering was changed.
- No business logic was changed.
- No mass CSS cleanup was attempted.
- No production visual redesign was attempted.
- No visual QA failures were suppressed by removing modules from coverage.
- No broad table/status/sidebar refactoring was done.

## Result

Corrective Baseline Phase 0 completed its target repairs. The original audit failures for whitespace, CSS duplicate, raw-token baseline, missing visual runtime module coverage, and stale `visualSystem` smoke contract are resolved.

Current baseline state is honest and reproducible:

- Nonvisual baseline gates pass: `build`, `qa:ui`, `qa:css`, `qa:architecture`, `qa:ui:regression`, `git diff --check`.
- Remaining failures are documented with exact reasons and next actions: `qa:visual`, `qa:functional`.
