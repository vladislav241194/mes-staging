# Stage 1: employee authentication only. Nomenclature command authority stays
# off until the separate Stage 2 drop-in is installed and accepted.
[Service]
EnvironmentFile=/etc/mes/mes-pilot-employee-auth.env
Environment=MES_ENABLE_EMPLOYEE_AUTH=1
