# Install together with the first System Domains command-surface drop-in.
# The allowlist itself stays outside the repository because it is deployment
# policy: create the referenced root-owned 0600 file from the accompanying
# example and replace the placeholder with the signed public actor ID.
#
# /etc/systemd/system/mes-pilot.service.d/49-system-domains-command-actors.conf
[Service]
EnvironmentFile=/etc/mes/mes-pilot-system-domains-command-actors.env
