[Service]
# PostgreSQL accepts and numbers the immutable revision before the browser
# produces any legacy-compatible projection. The flag remains a separate
# root-controlled rollout from work-order creation and attachment storage.
Environment=MES_ENABLE_SPECIFICATIONS2_SERVER_PUBLISH_COMMANDS=1
