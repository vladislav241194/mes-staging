[Service]
# A published Specifications 2.0 revision can create an idempotent work order
# in PostgreSQL. The API mirrors that committed projection to the compatibility
# snapshot until the planning read surface is fully server-authoritative.
Environment=MES_ENABLE_SPECIFICATIONS2_SERVER_COMMANDS=1
