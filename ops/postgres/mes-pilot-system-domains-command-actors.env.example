# Copy to /etc/mes/mes-pilot-system-domains-command-actors.env, set mode 0600,
# and add one signed principal in the public-auth form below.  Keep this line
# commented until it is replaced: a copied template must fail closed instead
# of accidentally authorizing a placeholder.
# MES_SYSTEM_DOMAINS_COMMAND_ACTORS=public:replace-with-public-auth-username
# Do not store passwords or session secrets here.
