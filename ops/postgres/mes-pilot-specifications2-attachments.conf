[Service]
# Controlled rollout: production files are stored in PostgreSQL and served
# only through the authenticated Specifications 2.0 attachment endpoint.
Environment=MES_ENABLE_SPECIFICATIONS2_ATTACHMENT_COMMANDS=1
